"""Unified Agent Harness：统一运行时组件集合。

对应需求：FR-HARNESS-01~06（ContextManager / ToolRegistry / ToolExecutor / PolicyEngine /
PermissionManager / ApprovalManager / SandboxManager / ObservationAdapter / RetryManager /
StateManager / TraceManager 统一复用）。
"""
